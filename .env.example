# ID Encoding Salt
# CRITICAL: Generate a secure random string for production use
# Example: openssl rand -base64 32
ID_ENCODING_SALT=your-secure-random-salt-here

# Add other environment variables below as needed